{
  "name": "@waganda/infrastructure",
  "version": "0.1.0",
  "private": true,
  "description": "와간다 AWS CDK 인프라",
  "scripts": {
    "cdk": "cdk",
    "synth": "cdk synth --all",
    "deploy": "cdk deploy --all --profile waganda --context env=prod --require-approval never",
    "diff": "cdk diff --all --profile waganda --context env=prod",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "aws-cdk-lib": "2.262.1",
    "constructs": "10.7.1",
    "@aws-sdk/client-bedrock-agentcore": "3.1097.0"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "aws-cdk": "2.1133.0",
    "ts-node": "10.9.2",
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
